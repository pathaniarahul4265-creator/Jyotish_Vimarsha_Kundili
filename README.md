# Jyotish Vimarsha v11

Production-oriented GitHub → Vercel build for Jyotish Vimarsha.

## Architecture

GitHub → Vercel → static `index.html` + serverless `/api/[...path].js` → Supabase + Razorpay + Gemini

See `SETUP.md` for the one-time setup.
